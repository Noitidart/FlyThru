import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    title_view: {
        flexDirection: 'row'
    },
    title_char: {
        marginHorizontal: 5,
        fontSize: 70,
        color: '#444444'
    }
})

export default styles