import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    title_view: {
        flexDirection: 'row',
        margin: 15,
        marginBottom: 30,
        alignItems: 'flex-end'
    },
    title_char: {
        marginHorizontal: 5,
        fontSize: 50,
        lineHeight: 70,
        color: '#444444'
    },
    title_char_big: {
        marginHorizontal: 5,
        fontSize: 60,
        color: '#444444'
    }
})

export default styles